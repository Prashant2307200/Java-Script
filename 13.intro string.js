// template literals

let frd = "Pramod";
let name = `${frd} is a friend of a Harry`;
console.log(name);
for(let i = 0; i < name.length; i++) {
    console.log(name[i]);
}

let sent = "I say quote \"old is gold\"";
console.log(sent);

let anotherSent = 'I say quote "old is gold"';
console.log(anotherSent);